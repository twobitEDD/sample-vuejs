export interface Networks {
    chainId: number;
    apiURL: string;
    rpcURL: string;
    explorer: string;
    name: string;
    hasPrice: boolean;
    delegatorAddress: string;
    delegatorContract: string;
    settingsContract: string;
}